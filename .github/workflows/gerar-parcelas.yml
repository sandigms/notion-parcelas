name: 🧱 Gerador de Parcelas Básico  # ← NOME ALTERADO

on:
  schedule:
    - cron: '*/10 * * * *'  # A cada 10 minutos
  workflow_dispatch:

jobs:
  gerar-parcelas-basico:  # ← NOME DO JOB ALTERADO
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Baixar código
        uses: actions/checkout@v3
      
      - name: 🐍 Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: 📦 Instalar dependências
        run: pip install -r requirements.txt
      
      - name: 🧱 Executar gerador básico  # ← NOME ALTERADO
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
        run: python gerador_parcelas.py  # ← Script básico
      
      - name: ✅ Concluído
        run: echo "Geração básica de parcelas concluída!"
